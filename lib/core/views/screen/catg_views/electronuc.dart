import 'package:flutter/material.dart';

class ElectronicView extends StatelessWidget {
  const ElectronicView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('electonic'),
      ),
    );
  }
}