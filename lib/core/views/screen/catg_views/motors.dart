import 'package:flutter/material.dart';

class MotorsView extends StatelessWidget {
  const MotorsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('motor'),
      ),
    );
  }
}