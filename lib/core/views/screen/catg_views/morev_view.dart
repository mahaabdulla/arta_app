import 'package:flutter/material.dart';

class MorevView extends StatelessWidget {
  const MorevView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('more'),),
    );
  }
}