import 'package:flutter/material.dart';

class RealStateView extends StatelessWidget {
  const RealStateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('real staete'),
      ),
    );
  }
}